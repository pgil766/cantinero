[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    integration: requiere PostgreSQL de docker compose (se omite si no está disponible)
    ollama: requiere Ollama con los modelos del .env descargados (se omite si no está disponible)
